# Neural Networks Visualizations

Application web interactive pour visualiser et comprendre les réseaux de neurones récurrents (RNN) et les LSTM (Long Short-Term Memory).

## 📚 Contenu

### 1. RNN Interactive (`rnn.html`)
- Visualisation étape par étape d'un RNN
- Calculs avec valeurs réelles
- Navigation par flèches clavier
- Visualisation du flux hₜ₋₁ → hₜ

### 2. RNN Architecture (`rnnshema.html`)
- Schéma détaillé de l'architecture RNN
- Visualisation du problème de disparition du gradient
- Explications mathématiques complètes
- Connexions animées

### 3. LSTM Interactive (`lstm.html`)
- Visualisation complète d'un LSTM unrollé
- 4 timesteps avec calculs détaillés
- Portes (Forget, Input, Output, Candidate)
- Cell State et Hidden State
- Navigation par flèches clavier

### 4. LSTM Architecture (`lstmshema.html`)
- Architecture interne d'une cellule LSTM
- Flux de données animé
- Toutes les formules expliquées
- Connexions visuelles claires

## 🚀 Déploiement sur Vercel

1. Installer Vercel CLI (si pas déjà installé):
```bash
npm i -g vercel
```

2. Se connecter à Vercel:
```bash
vercel login
```

3. Déployer:
```bash
vercel
```

Ou simplement pousser sur GitHub et connecter le repo à Vercel depuis le dashboard.

## 🎯 Utilisation

Ouvrez `index.html` dans votre navigateur ou déployez sur Vercel pour accéder à toutes les visualisations depuis une page d'accueil centralisée.

## 📖 Fonctionnalités

- ✅ Calculs en temps réel avec valeurs réelles
- ✅ Visualisations interactives étape par étape
- ✅ Navigation au clavier (flèches)
- ✅ Explications détaillées et formules
- ✅ Visualisation des problèmes (disparition du gradient)
- ✅ Schémas architecturaux complets

